{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build builddb",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/solution/builddb"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}
